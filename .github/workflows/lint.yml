name: Lint

on:
  pull_request:
  push:
    branches:
      - main
    paths:
      - 'hy2.sh'
      - 'install.sh'
      - 'README.md'
      - 'CHANGELOG.md'
      - 'scripts/**'
      - 'tests/**'
      - '.editorconfig'
      - '.gitattributes'
      - '.gitignore'
      - '.github/workflows/*.yml'

jobs:
  verify-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y shellcheck bats

      - name: Make verify script executable
        run: chmod +x scripts/verify.sh scripts/check-style.sh scripts/check-menu-sync.sh scripts/check-version-sync.sh scripts/check-release-package.sh scripts/smoke-e2e.sh tests/e2e/config-flow.sh

      - name: Run bash syntax checks
        run: ./scripts/verify.sh syntax

      - name: Check repository style
        run: ./scripts/verify.sh style

      - name: Run shellcheck
        run: ./scripts/verify.sh shellcheck

      - name: Check menu sync
        run: ./scripts/verify.sh menu-sync

      - name: Check version sync
        run: ./scripts/verify.sh version-sync

      - name: Check release package guardrails
        run: ./scripts/verify.sh release-package

      - name: Run smoke E2E
        run: ./scripts/verify.sh smoke-e2e

      - name: Run bats tests
        run: ./scripts/verify.sh bats

      - name: Run config flow replay
        run: ./scripts/verify.sh config-flow

  verify-debian:
    runs-on: ubuntu-latest
    container: debian:12
    steps:
      - name: Install dependencies
        run: apt-get update && apt-get install -y bash git shellcheck bats grep sed gawk coreutils

      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Make verify script executable
        run: chmod +x scripts/verify.sh scripts/check-style.sh scripts/check-menu-sync.sh scripts/check-version-sync.sh scripts/check-release-package.sh scripts/smoke-e2e.sh tests/e2e/config-flow.sh

      - name: Run bash syntax checks
        run: ./scripts/verify.sh syntax

      - name: Check repository style
        run: ./scripts/verify.sh style

      - name: Run shellcheck
        run: ./scripts/verify.sh shellcheck

      - name: Check menu sync
        run: ./scripts/verify.sh menu-sync

      - name: Check version sync
        run: ./scripts/verify.sh version-sync

      - name: Check release package guardrails
        run: ./scripts/verify.sh release-package

      - name: Run smoke E2E
        run: ./scripts/verify.sh smoke-e2e

      - name: Run bats tests
        run: ./scripts/verify.sh bats

      - name: Run config flow replay
        run: ./scripts/verify.sh config-flow
